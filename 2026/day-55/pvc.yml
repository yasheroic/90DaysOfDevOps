kind: PersistentVolumeClaim
apiVersion: v1

metadata:
    name: pvc-1

spec:
    storageClassName: ""
    accessModes:
        - ReadWriteOnce
    resources:
        requests:
            storage: 500Mi