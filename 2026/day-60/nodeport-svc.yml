kind: Service
apiVersion: v1

metadata:
    name: nodeport-svc

spec:
    type: NodePort
    selector:
        app: wordpress-app
    ports:
        - port: 80
          targetPort: 80
          nodePort: 30080